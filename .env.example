PORT=3000
HOST=0.0.0.0
ACCESS_PIN=7875

# Add this on Render when you want permanent cloud storage.
# Use your Supabase Postgres connection string.
DATABASE_URL=

# Leave DATA_FILE blank when DATABASE_URL is set.
DATA_FILE=./data/queue.json

# Leave these blank when the frontend and backend are on the same Render URL.
PUBLIC_API_BASE_URL=
CORS_ORIGIN=
